.container
  .row
    %h1= "#{current_user.name}"
    .col-md-6
      %h3 My Topics
      - current_user.topics.each do |topic|
        = link_to "#{topic.title}", topic
        %br/
        - topic.bookmarks.each do |bookmark|
          = link to "#{bookmark.url}", bookmark.url
          %br/
    .col-md-6
      %h3 Topics and Bookmarks I Like